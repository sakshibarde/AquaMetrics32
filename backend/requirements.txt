flask
flask-cors
flask-compress
flask-caching
gunicorn
pandas
requests
plotly
matplotlib
psycopg2-binary
apscheduler

# Pinned to avoid numpy 2.x breaking TensorFlow and to match saved model artifacts
numpy<2.0.0
scikit-learn==1.6.1
joblib

# h5py must be pinned — mismatches cause segfaults when loading .h5 models
h5py==3.11.0

# tensorflow-cpu pinned: >=2.16 uses standalone Keras 3 (keras package).
# Do NOT use bare `from keras.models import ...` — always use tensorflow.keras
tensorflow-cpu==2.16.2
